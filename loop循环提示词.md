# 新亦源官网全站检查与 Loop 循环提示词

更新时间：2026-08-09
项目目录：`/home/yj/XYY-GEO/website`
检查对象：本地候选版本、验收域名 `https://wz.tomatopia.top`、正式域名 `https://56xyy.com`、Directus CMS、当前 PostgreSQL 数据库及 Oracle 19C 迁移脚本。

## 一、本次全站检查方案

### 1. 建立基线

- 阅读 `design.md`、`README.md`、`CLAUDE.md`、`docs/PROJECT_STATUS.md`、`src/lib/claims.ts`。
- 记录当前分支、未提交文件、环境变量名称、运行端口和部署边界。
- 区分三种状态：本地候选版本、验收服务器版本、正式主站版本，禁止把“本地已完成”误写成“线上已发布”。

### 2. 前端静态检查

- 执行 Astro 类型检查、ESLint、Vitest、生产构建和依赖安全审计。
- 检查路由、标题、H1、description、canonical、语言声明、JSON-LD、robots、sitemap、llms.txt。
- 检查全站业务口径是否统一，包括 `54万㎡`、`150+品牌`、`1500+员工`、`45万+ SKU` 等。
- 检查图片、视频、字体和 PDF 的体积、格式、缓存与是否仍被页面引用。

### 3. 真实浏览器检查

- 使用 Chromium 检查全站路由，不只检查首页。
- 桌面端至少覆盖 1366、1440；移动端至少覆盖 360、390、430；补充 768、1024。
- 检查横向溢出、图片加载、控制台错误、交互、键盘操作、焦点、菜单、FAQ、弹窗、CTA 和表单错误提示。
- 检查页面截图与 DOM 语义是否一致，不能只凭视觉截图判断。

### 4. 后端与数据链路检查

- 检查 `/healthz`、Directus `/server/ping`、联系表单 API 和异常返回。
- 检查 Directus 进程、版本、权限、限流、CORS、日志和数据表数量。
- 检查数据库类型、数据库大小、备份任务、恢复能力和迁移状态。
- Oracle 19C 未连接真实数据库时，只能标记“脚本静态检查通过”，不得写成“迁移完成”。

### 5. 服务器与发布检查

- 检查 PM2、Nginx、端口监听、磁盘、内存、TLS、安全响应头和缓存策略。
- 检查部署是否原子化，避免直接覆盖正在运行的 `dist/`。
- 检查验收域名和正式域名是否指向正确版本，canonical、robots、sitemap、llms.txt 是否同步。
- 默认不执行部署、DNS切换、数据库写入、Git提交或推送。

### 6. 性能、无障碍和安全检查

- 对首页和关键转化页执行 Lighthouse。
- 检查 LCP、CLS、TBT、传输体积、颜色对比度、可访问名称和 DOM 规模。
- 执行 `npm audit`，区分运行时依赖与开发依赖，不盲目自动升级大版本。
- 检查生产源站是否绕过 Nginx 暴露、表单是否可能静默丢数据、数据库是否有可恢复备份。

## 二、本次检查结果摘要

### 已通过（本地候选版本）

- 当前候选版本通过：Astro检查257个项目输入零诊断、ESLint、Prettier、391个项目文件可维护性预算、55个本地资源引用跨329个源码文件、48项 Vitest 与生产构建全部通过。
- `npm run test:e2e` 已纳入 `npm run verify:release`；27项通过、3项按移动项目配置有意跳过，覆盖首页、产品目录、360/390/430/768/1024/1366/1440响应式、12条服务专题、菜单/弹窗键盘关闭、SEO/AEO、后端失败关闭、案例、期刊、运到、表单与新闻状态。
- `npm run test:formal-contract` 也已纳入 `verify:release`；3项运行态测试会以正式源站配置重新构建，验证无`noindex`、正式canonical/OG URL、robots/sitemap/llms.txt以及www、旧域名和旧路径跳转。
- `npm audit --omit=dev` 为0项已知漏洞；未使用 `--force` 破坏性修改 Lighthouse 开发工具链。
- 联系表单缺少 Directus 配置时返回503；`/healthz` 会真实请求 Directus `/server/ping`，不可达时返回503，不再静默成功。
- `/product` 360px溢出、服务目录 Tab/Panel ARIA与键盘导航、移动菜单 Escape、空图片 `src`、CTA对比度、案例名称与 Cases H1 均已在本地修复并有回归测试。
- 关于页低高度桌面视口中，底部数据条曾拦截静音按钮；现已预留内容空间、纠正层级，并以正常用户点击和ARIA状态变化回归，不使用强制点击绕过。
- 部署脚本已改为版本目录、`current` 原子软链、健康检查、失败回滚与最近5版保留；仅完成本地静态和构建验证，尚未在服务器实跑。
- PostgreSQL备份、systemd timer和恢复测试脚本已加入 `deploy/postgresql/`；尚未安装到服务器，也没有真实恢复演练记录。
- 4.6MB的 `warehouse-aisle-202.jpg` 已替换为214KB WebP并删除旧资源。
- 2026-08-09完成职责拆分并重新执行 `npm run verify:release`：257个Astro输入零诊断、391个维护文件、55个资源引用跨329个源码文件、48项Vitest、27项E2E（3项有意跳过）、3项正式域名契约和两次生产构建全部通过；生产依赖审计为0，格式与差异检查通过。
- 联系API新增无效邮箱拒绝契约，页面字段长度与服务端一致，提交结果可由读屏实时播报；正式发现文件契约新增Content-Type与`54万㎡ / 150+品牌 / 45万+ SKU`口径断言；Oracle准备脚本新增Directus Oracle驱动与Thin模式前置检查。
- 健康响应与外部检查统一使用`dependencies.contactStorage: ok`，并以纯函数契约拒绝旧格式、降级和不可达状态；修复了健康服务正常但检查脚本因枚举不一致误触发回滚的发布级缺陷。
- 同日线上只读复核显示：验收站仍是旧候选版，旧 `/healthz` 只有 `{"status":"ok"}`、缺少依赖状态字段，严格检查无法证明联系存储健康；`.env`必需键均存在，`/llms.txt` 为404。PM2 Web/CMS在线、Nginx语法正常、Directus 12.1.1仍连接约9.8MB PostgreSQL；服务器仍直接运行`dist/`而没有`current`原子软链，PostgreSQL备份timer和备份目录均不存在。正式域名仍解析到旧服务器，健康、CMS、robots、sitemap和llms路径均被旧站回退HTML，不能因HTTP 200误判为可用。
- 2026-08-09 15:32再次只读复核：上述外部状态未变化；正式域名四个发现/健康路径仍是882字节的旧站HTML回退页，验收站发现文件仍缺失。除非获得部署、数据库和DNS授权，不得继续把本地成功描述成线上成功。

### P0：仍需外部环境或授权

1. 在服务器安装数据库备份任务，完成异机保留和一次真实恢复演练。
2. 在验收环境执行原子部署和失败回滚演练，部署新版依赖健康检查与 `/llms.txt`；当前不能把“脚本已写”描述为“线上已完成”。
3. 完成 `56xyy.com` 的DNS、证书、Nginx、环境变量、canonical、robots、sitemap、表单和CMS验证后再切换。
4. Oracle 19C迁移必须取得真实独立数据库服务器、官方介质和凭据，并在8056并行实例完成数据核对、备份和恢复验证后才可切换。

### P1：本地剩余质量项

1. Lighthouse桌面三次采样中位数：首页性能83、产品页91、关于页90；LCP分别2.16秒、1.80秒、1.93秒，最佳实践均100。产品页最终单次复验为性能92、无障碍100、最佳实践100、LCP 1.8秒、TBT 0、CLS 0。三页SEO为69是本地候选环境 `noindex` 所致，正式域名上线后仍须多次采样复测。
2. 产品首屏已使用960/1440两档响应式WebP；`prepare:fonts` 从源码提取1338个码点，为普惠体400/900各生成一个约200KB站点子集。桌面异步启用品牌字体，移动端使用系统中文字体，字体请求降为0；非首屏动效仅在首次交互后初始化。在用履约图片已从1.69MB JPG切换为270KB WebP，关于页首屏视频已从17MB/720p切换为7.5MB/540p优化版；约43MB未发布原始媒体已移入 `resources/original-media/unpublished-public/`，仍可恢复。
3. 验收域名 `/llms.txt` 仍取决于下一次部署；本地路由与构建产物已存在。
4. Lighthouse CLI 的开发依赖仍有 `tmp`/`uuid` 公告，只能通过破坏性降级建议处理；生产依赖审计为0，暂不执行 `npm audit fix --force`。

### P2：持续改进项

1. Directus 升级须在备份和兼容测试后进行。
2. 验收环境 `noindex,nofollow` 合理；正式域名上线必须确认移除。
3. 首页 DOM 和 ScrollTrigger 仍可继续减负，但必须保持当前视觉和交互契约。
4. 最新移动端采样：首页86/模拟LCP3.7秒、产品页77/6.3秒、关于页76/5.6秒；真实追踪LCP约1.63秒、1.33秒、1.36秒。正式环境仍需按冷缓存、暖缓存和真实移动网络多次确认。

### 可维护性专项：本轮实际拆分与剩余债务

已完成第四轮职责拆分，不再把已完成项留在“下一批”：

- 首页新增 `HomeHero`、`HomeCapabilityStats`、`HomeCoreSolutions`、`HomeCases`、`HomeCaseModal`、`HomeFulfillment`、`HomeFAQ`；配置进入 `src/data/home.ts`，浏览器逻辑按Hero/滚动、能力区状态机和服务模块动画拆分。
- `index.astro` 从最初3876行降到117行，只保留CMS取数、Schema和页面编排；失效的旧产品卡片CSS已删除。
- 产品页目录、服务系列、商品整理、服务流程和保障机制分别迁入五个业务组件；`src/data/product.ts`只保留兼容出口，服务系列、业务问题和养护/保障进入独立内容模块，页面样式和交互继续由 `product.css`、`product-page.ts` 维护。
- `product.astro` 从799行降到138行；15行延迟加载器与103行动效控制器独立，目录的ARIA、键盘行为以及拆分后1366/1440和360宽度由E2E锁定。
- 服务专题共享类型、体验文案和签名配置进入三个独立内容模块，`src/data/service.ts`保留稳定导出；Hero和详情/FAQ/CTA进入独立组件；`ServiceLanding.astro` 从765行降到105行，12条路由由桌面/移动E2E统一验证。
- `product.css` 原1395行与 `service-signature.css` 原1632行已按产品职责、专题视觉变体和响应式规则分片；年限卡、JIT雷达、广州枢纽、服务详情和首页流程又按卡片/路线、视觉/轨道、地图/详情、布局/变体、卡片/说明继续拆分，聚合入口只保留有序导入；当前最大CSS按门禁计数为147行。
- `ServiceSignature.astro` 的12种模板已按仓储/区域、数字履约/跨境/寄件、质检/修复迁入三个视觉族组件，父组件从664行降到当前62行；12条路由桌面/移动契约继续通过。
- 关于页Hero、历程、仓网、仓点和荣誉完成组件化；发展历程、仓网规则和FAQ进入独立内容模块，`src/data/about.ts`保留兼容出口；`about.astro` 从565行降到当前94行。
- 案例页Hero、Logo墙、案例网格和价值模块完成组件化，配置进入 `src/data/cases.ts`；`cases.astro` 从527行降到当前70行。
- 期刊目录与统计/FAQ拆成独立内容模块，`src/data/publications.ts`只保留兼容出口；联系页Hero/联系方式/表单/控制器分离；页面入口当前分别为75行和53行。
- 首页方案、能力、媒体、履约与FAQ进入5个独立内容模块；案例Logo、价值说明和FAQ进入3个独立内容模块，原`home.ts`和`cases.ts`保留稳定门面。关于页视频/历程/计数控制器独立，案例弹窗视图渲染与生命周期控制独立。
- 运到专题独有界面迁入 `YundaoOperations`；页面从410行降到当前80行。
- `ProductServiceDirectory` 的模板、键盘控制器和样式分离，组件从461行降到当前93行；首页能力区864行样式拆为职责分片。

已成立的职责边界与剩余债务：

- 当前页面入口不超过150行、Astro业务组件不超过180行、公开API不超过100行、单个CSS不超过200行、内容数据文件不超过180行；12种服务视觉模板和12个专题独有模块均已隔离。预算只是防回退上限，不能代替职责评审。
- 首页Hero、FAQ和履约模块已完成模板、数据、样式、控制器分层；能力统计拆为6个子组件，原374行动画脚本拆为运行时、年限卡、统计卡、流程线和72行入口协调器。
- 产品服务系列拆为父编排、单项、画廊和图标；12套服务专题视觉变体进入12个独立组件，三族父组件只负责分派。
- Header拆为桌面导航、移动导航、专题链接配置与菜单控制器；About荣誉Lightbox与通用FAQ完成样式/交互分离。
- Directus由单文件拆为类型、客户端、查询和兼容门面；品牌数据和业务声明也改为领域模块＋兼容门面；物流数字化页411→48行、案例详情入口147→58行、新闻详情路由282→65行、新闻列表入口209→58行，正文主体与相关推荐独立，12条专题入口统一为73–80行。
- 联系API由179→43行，请求读取、限流、字段校验和Directus存储分别维护；CMS初始化176→42行，集合模型与管理运行时独立；审核同步224→91行，审核事实数据、差异/备份、语义业务键匹配与仓点规则独立。
- Oracle内容迁移拆为109行编排、Directus传输客户端和纯数据规范化/哈希；跨库技术主键重新生成，原始快照与实际传输快照分别留存，遇到自定义关系字段时失败关闭。CMS服务器初始化Shell也已拆成约47行入口与函数库。
- 履约事实不再将运营绩效与企业规模放在同一注册表：两类事实分别维护，`fulfillment.ts`保留稳定合并出口；期刊清单等单一事实集合不为行数机械拆分。
- `check:maintainability` 已纳入 `npm run verify`，当前扫描391个项目文件全部通过；检查必须同时回答页面编排、业务组件、内容数据、浏览器控制器、样式、CMS读取/管理、API、Web运行时、构建工具与部署脚本是否只有一个变化原因，不能只报行数。
- 已继续拆分关于页Hero的媒体/控制/数据条、联系表单的身份/咨询/隐私字段、华南网络的节点/能力带/内容配置，以及退货修复、JIT对比和直播挑战的数据与视图。`server.mjs` 只装配应用，运行配置、请求策略和健康探测分别维护；字体准备和Oracle准备也已拆为编排入口与职责函数。
- 当前观察项改为147行能力区手机断点、147行服务签名共享基础、146行产品系列展开样式，以及113行新闻列表导语、111行关于页历程、111行页脚；它们仍各自只有一个变化原因，出现第二种断点、数据源、布局或交互状态机时必须先拆。
- Directus不可用时，CMS统计与三项CMS服务返回空集合；当前不采用陈旧快照回退，因此CMS健康检查必须作为发布硬门槛，是否启用审核快照应单独决策。

下一阶段顺序：正式环境多次性能复测 → 真实服务器备份/恢复与部署演练 → 正式域名切换验证。两份接近预算上限的CSS已经按变体拆分；后续每次只移动一个职责边界，保持视觉、文案、接口和路由不变，并在每步运行发布门禁。

### 数据与环境现状

- CMS当前数据库：PostgreSQL，不是Oracle。
- 内容数量：`homepage_stats=8`、`services=3`、`warehouses=12`、`cases=6`、`news=0`、`contact_leads=0`。
- Directus公开内容接口默认禁止匿名访问，前端构建依靠Token读取；这是当前权限设计，不应误报为接口故障。
- Oracle 19C迁移脚本尚未在真实独立数据库服务器执行，当前状态只能标记为“待实迁和恢复验证”。

## 三、建议执行顺序

1. 在正式环境以冷缓存、暖缓存和移动网络分别复测首页、产品页和关于页性能。
2. 取得服务器授权后安装 PostgreSQL 备份任务，完成异机备份与恢复演练。
3. 在验收服务器实跑原子部署和回滚，验证 `/llms.txt`、全站路由、表单和CMS。
4. 完成正式域名切换清单后再上线 `56xyy.com`。
5. Oracle 19C只在独立数据库服务器就绪、备份与并行核对通过后实施。

---

## 四、可直接复制使用的 Loop 循环提示词

```text
你现在负责对 `/home/yj/XYY-GEO/website` 进行一次“审计—修复—验证—复审”的闭环迭代。

目标：在不破坏用户现有改动、不伪造业务数据、不擅自部署或推送的前提下，持续处理最高优先级问题，直到达到发布门槛，或遇到确实需要用户授权/外部环境变化的阻塞。

一、开始前必须读取
1. 完整阅读 `design.md`。
2. 阅读 `README.md`、`CLAUDE.md`、`docs/PROJECT_STATUS.md`。
3. 阅读 `src/lib/claims.ts`，它是业务口径的重要来源。
4. 检查 `git status --short`，所有既有修改都视为用户资产，不覆盖、不回滚。
5. 区分本地候选版本、`wz.tomatopia.top` 验收版本、`56xyy.com` 正式版本，不混淆状态。

二、权限边界
- 默认允许：读取源码、运行检查、启动本地服务、使用真实浏览器、修改当前任务明确授权范围内的文件。
- 默认禁止：部署服务器、切换DNS、写生产数据库、提交真实联系表单、删除生产数据、Git commit/push、修改GitHub状态。
- 只有用户明确授权后，才能执行部署、数据迁移、域名切换、提交或推送。
- 不输出 `.env`、Token、密码、数据库连接串等秘密。
- Oracle没有真实连接和验证记录时，只能写“脚本检查通过”，不得写“迁移完成”。

三、每轮固定流程

第1步：读取当前状态
- 查看本轮前后的 `git diff --stat` 与相关文件差异。
- 确认问题可稳定复现，记录路由、视口、请求、日志或失败测试。
- 如果证据不足，先补检查，不凭感觉改代码。
- 统计目标文件行数、内联CSS/JS、事件监听、重复数据和共享组件使用情况，判断结构债务。

第2步：选择一个最高优先级问题
- P0：数据丢失、安全、发布失败、主流程不可用、测试门禁失真、无法回滚。
- P1：移动端溢出、关键交互/无障碍错误、SEO语义错误、明显性能问题、线上本地漂移。
- P2：文档、轻量优化、依赖升级、代码整洁度。
- 每轮优先解决一个根因，避免同时大改多个模块。

第3步：做最小修改
- 只修改与根因直接相关的文件。
- 使用 `apply_patch` 编辑文件。
- 不顺带重构，不改业务文案和数据口径，不改变未授权的设计结构。
- 保留响应式、键盘访问、减少动态效果模式和现有交互兼容性。
- 若本轮是模块拆分，只移动一个独立业务模块；不得同时改文案、视觉、接口和路由。
- 拆分前锁定现有截图和交互测试，拆分后做等价验证。

第4步：针对性验证
- 先跑与改动最相关的测试。
- 前端视觉问题必须用真实浏览器在对应路由和宽度复测。
- 后端问题必须验证成功、失败、缺配置和依赖不可用路径。
- 数据库/部署脚本先执行静态检查；真实执行前必须有备份、回滚和用户授权。

第5步：全量回归
至少执行：
  npm run verify
  npm run test:e2e
  npm audit --omit=dev

浏览器至少检查：
- 路由：`/`、`/product`、`/cases`、`/about`、`/contact`。
- 宽度：360、390、430、768、1024、1366、1440。
- 项目：状态码、控制台错误、横向溢出、图片、菜单、FAQ、CTA、表单、焦点、Escape、键盘Tab。

SEO/AEO至少检查：
- 每页一个H1、title、description、canonical、lang。
- JSON-LD可解析。
- robots、sitemap、llms.txt状态正确。
- 验收域名允许noindex；正式域名禁止noindex。

后端至少检查：
- `/healthz`、Directus `/server/ping`。
- 联系表单校验、限流、CMS不可用和缺环境变量路径。
- PM2、Nginx、磁盘、内存、错误日志。
- 数据库备份存在，并且有恢复验证记录。

可维护性至少检查：
- 页面、组件、测试和运维脚本行数，按Astro组件180行、页面入口150行、CSS 200行、前端TypeScript 260行、内容数据180行、公开API 100行、测试220行、MJS 200行、CMS初始化入口100行、审核同步/Oracle迁移入口140行、CJS 180行、Shell 200行预算识别高风险文件。
- 页面内联CSS超过180行、内联脚本超过100行的文件；自闭合JSON-LD脚本不得被误计为内联代码块。
- 重复业务数据、流程、FAQ、CTA和响应式规则。
- DOM选择器、事件监听与ARIA契约是否有对应测试。
- 文档描述、测试断言与当前页面结构是否同步。
- 共享组件按业务职责拆分，不为减少行数过度碎片化。
- 不得只凭“低于行数预算”标记已完成：逐项检查页面入口、业务组件、事实/内容数据、浏览器控制器、样式、CMS读取、CMS管理、公开API、测试与发布脚本是否能够独立理解、修改、测试和回滚。
- 对每个职责域使用 `门禁通过 / 观察 / 待外部验证` 三种状态；“门禁通过”仅代表本地代码与自动检查成立，不得冒充服务器或数据库实测。

第6步：比较并决定是否继续
- 如果本轮问题已修复且没有新增回归，进入下一项最高优先级问题。
- 如果相同错误连续三轮仍无法推进，停止修改，说明阻塞证据和所需授权。
- 如果出现新P0，立即停止低优先级工作，先处理P0。

四、当前已知问题队列
1. 已完成并需持续回归：发布门禁、表单失败关闭、360px溢出、ARIA Tab、Escape、空src、CTA对比度、Cases H1和生产依赖审计。
2. 已准备但需服务器授权：PostgreSQL备份/恢复、原子部署/回滚、部署新版依赖健康检查和 `/llms.txt`；当前线上严格健康检查已明确失败。
3. 可维护性已建立可执行预算和职责状态矩阵：页面入口、共享Layout、首页状态机、Header、产品系列、Directus读取/管理、联系API、品牌事实与声明、物流数字化、案例/新闻、Web运行时、字体构建、Oracle迁移与准备、CMS服务器初始化、12套服务视觉、12个专题独有模块、候选/正式域名E2E和部署脚本均按职责治理；当前391个项目文件全部通过，最大CSS按门禁计数为147行。最大样式、业务组件、内容、测试与发布入口保持“观察”，不能因门禁通过就写成无需维护。
4. 性能下一批：在正式环境按冷/暖缓存和移动网络多次复测，并重点比较 Lighthouse 模拟值与真实追踪值；本地最新桌面单次采样为首页97、产品页97、关于页99。
5. 外部上线：正式域名DNS、证书、Nginx及SEO/AEO一致性检查。
6. 数据库迁移：Oracle 19C真实环境并行验证、数据核对、备份、恢复和回滚。

五、完成门槛
- P0为0，或明确列出需要用户授权的外部阻塞。
- `npm run verify` 与 `npm run test:e2e` 全部通过。
- 核心路由无404、无控制台错误、无360px横向溢出。
- 关键交互支持鼠标、触摸和键盘。
- 联系表单不会静默丢数据。
- CMS和数据库有健康检查、备份和可验证恢复路径。
- 正式域名上线时无 `noindex`，canonical、robots、sitemap、llms.txt一致。
- 变更文件、测试证据、剩余风险和回滚方法都已记录。
- 核心页面不再继续增长为单文件应用；新功能进入职责清晰、可独立测试的模块。

六、每轮输出格式

## Loop 第N轮
- 本轮目标：
- 复现证据：
- 根因：
- 修改文件：
- 修改内容：
- 针对性验证：
- 全量回归：
- 新增风险：
- 剩余P0/P1/P2：
- 下一轮建议：
- 是否需要用户授权：否 / 是（写明具体动作）

不要只说“已优化”或“应该没问题”。所有结论都必须附带测试、浏览器、HTTP、日志或源码证据。
```

## 五、建议保留的审计命令

```bash
npm run verify
npm run test:e2e
npm audit --omit=dev
SITE_URL=https://wz.tomatopia.top node scripts/health-check.mjs
bash -n deploy/oracle19c/*.sh
node --check deploy/oracle19c/migrate-directus-content.mjs
```

真实浏览器检查应优先使用项目现有 Playwright 配置和本地生产构建，不要只用开发服务器截图代替发布候选版本。
