# 可维护性状态矩阵

更新时间：2026-08-16

## 当前结论

本地候选版本已经从“页面单文件应用”收敛为页面编排、业务组件、内容数据、浏览器控制器和样式域五层结构。可维护性不再只靠人工约定：`npm run check:maintainability` 会扫描源码并阻止文件或内联代码重新失控，该门禁已纳入 `npm run verify`。

门禁覆盖 `src/`、`server/`、`scripts/`、`deploy/`、`tests/` 和根级运行配置；最新文件数量与验证结果以根目录 `DEV_STATE.md` 为准。当前预算：

- Astro 组件预算：180 行，页面入口另收紧至 150 行；
- CSS 文件预算：200 行；
- 前端 TypeScript 文件预算：260 行，`src/data/` 内容文件另收紧至180行，公开 API 路由另收紧至 100 行；
- 测试 TypeScript 文件预算：220 行；
- MJS 自动化脚本预算：200 行，CMS初始化入口100行、审核同步与Oracle迁移入口140行；
- CJS 文件预算：180 行，Shell 文件预算：200 行；
- Astro 内联样式预算：180 行；
- Astro 内联脚本预算：100 行；
- 当前所有预算均通过。

预算是防止回退的上限，不代表达到上限才拆分。是否拆分仍以“能否独立理解、修改、测试和回滚”为判断标准。

## 页面入口

| 入口                                    | 当前行数 | 职责                            | 状态   |
| --------------------------------------- | -------: | ------------------------------- | ------ |
| `src/pages/index.astro`                 |      117 | CMS取数、Schema、首页编排       | 已收敛 |
| `src/pages/product.astro`               |      138 | 产品配置、Schema、产品编排      | 已收敛 |
| `src/pages/about.astro`                 |       94 | CMS仓点取数、Schema、关于页编排 | 已收敛 |
| `src/pages/cases.astro`                 |       70 | CMS案例取数、Schema、案例页编排 | 已收敛 |
| `src/pages/cases/[slug].astro`          |       58 | 案例取数、Schema、详情模块编排  | 已收敛 |
| `src/pages/contact.astro`               |       53 | 联系页Schema与模块编排          | 已收敛 |
| `src/pages/senlinqikan.astro`           |       75 | 期刊Schema与模块编排            | 已收敛 |
| `src/pages/yundao-zhineng-jijian.astro` |       80 | 专题配置与独有模块编排          | 已收敛 |
| `src/pages/wuliu-shuzihua.astro`        |       48 | 数字化专题Schema与模块编排      | 已收敛 |
| `src/pages/news/index.astro`            |       58 | 新闻Schema、空状态与FAQ编排     | 已收敛 |
| `src/pages/news/[slug].astro`           |       65 | 新闻取数、Schema与正文编排      | 已收敛 |
| `src/layouts/ServiceLanding.astro`      |      105 | 12条专题共享Schema和模块编排    | 已收敛 |

12 条专题路由现为 73–80 行，只保留页面元数据、业务配置、FAQ和独有模块装配。每条专题的独有可视化均已进入 `src/components/service/unique/`，不再把页面配置与大段 DOM 混在同一入口。

## 首页

首页入口只编排以下业务模块：

- `HomeHero`：37 行模板；样式在 `home-hero.css`；
- `HomeCapabilityStats`：61 行父编排；年限、仓储、网络、质检、准确率、生命周期拆为 6 个子组件；
- `HomeCoreSolutions`：20 行父编排；核心方案、数字产品和能力列表独立；
- `HomeCases` / `HomeCaseModal`：画廊、弹窗 DOM、弹窗样式和交互控制器分离；
- `HomeFulfillment`：42 行模板；步骤进入 `src/data/home.ts`，样式与 IntersectionObserver 控制器分离；
- `HomeFAQ`：55 行模板；手风琴样式和互斥控制器分离。

原 374 行 `home-capability-motion.ts` 已继续拆为：

- `home-capability-motion.ts`：72 行，只负责 DOM 装配和 ScrollTrigger 注册；
- `home-capability/runtime.ts`：数字解析、计数和 Timeline 生命周期；
- `home-capability/tenure-motion.ts`：年限卡动画；
- `home-capability/card-motion.ts`：统计卡动画；
- `home-capability/flow-motion.ts`：生命周期线动画。

这些模块共享同一运行时，但可分别修改和回归，不再把所有动画细节放在入口脚本。

## 产品页

产品页当前边界：

- `ProductServiceDirectory`：目录 DOM；键盘 Tab 控制器独立，布局进一步拆为导语、问题列表和方案面板，响应式规则独立；
- `ProductServiceSeries`：40 行父编排；单项展示、商品整理画廊和服务图标独立；
- 产品服务系列样式按导语、通用展开、基础仓配、退货处理和商品养护拆为 5 个文件，单文件 70–156 行；
- `ProductCare`、`ProductServiceProcess`：商品整理和流程；
- `ProductAssurance`：46 行内容编排；五套 SVG 图标进入 `ProductAssuranceIcon`；
- `src/data/product.ts` 只保留兼容导出，服务系列、业务问题和养护/保障配置分别位于 `src/data/product/`；
- 首次交互加载器在14行 `src/scripts/product-loader.ts`，页面动效控制器在103行 `src/scripts/product-page.ts`；固定4秒自动加载已删除，避免非交互访问抢占首屏渲染；
- `product.css` 只保留分片导入。

## 服务专题

12 条服务专题共用 `ServiceLanding`、`ServiceLandingHero`、`ServiceExperience` 和 `ServiceSignature`。本轮继续处理了此前仍然隐藏在三族组件内的变体耦合：

- 仓储族 6 个变体：Journey、Showroom、广州枢纽、华南网络、华东半径、门店节奏；
- 数字族 4 个变体：运单台、跨境塔、直播指挥、JIT雷达；
- 质检族 2 个变体：证据实验室、修复工坊。

现在每个签名变体均位于 `src/components/service/signature/` 的独立组件，三族父组件均低于 35 行，只负责分派。12 条专题的二级独有模块也分别位于 `src/components/service/unique/`，页面入口统一收敛至 73–80 行。修改某一专题的 DOM 不再需要进入包含页面Schema、FAQ和其他视觉变体的大文件。

样式继续共享设计变量，但不再把不同视觉变体堆在同一个文件：

- 原 `apparel-network.css` 已按 Showroom、广州枢纽、华南网络、华东半径和门店节奏拆为 5 个文件，单文件 76–166 行；
- 原 `service-series.css` 已按导语、通用展开及 3 类产品变体拆为 5 个文件，单文件 70–156 行；
- 原 `home-core-solutions.css` 已按基础布局、数字系统、智能寄件和正文能力拆为 4 个文件；
- 原 `service-experience.css` 已按详情、FAQ/CTA和响应式拆分，详情又按基础结构与专题变体分离；
- 原332行 `logistics-digital.css` 已按Hero、系统模块、协同链、证明条和响应式拆为5份（30–108行）；
- 原328行 `product/responsive.css` 已按平板、Hero、服务系列、养护流程、保障CTA和减少动态拆为6份（9–91行）；
- 首页案例按区块标题、画廊面板、详情交互和响应式拆分，案例弹窗按外壳、首图、正文和响应式拆分；首页能力统计按布局、年限卡、指标内容和装饰拆分；首页Hero的稳定入口继续分成内容布局、行动/滚动提示和响应式，履约区按布局、步骤和响应式拆分；直播履约和JIT雷达样式不再共存于同一文件；
- 运到运营页按标题、界面、流程场景与响应式拆分；首页能力指标的通用排版与业务卡片变体分离，能力区响应式进一步按平板、手机和减少动态分片；产品基础样式按共享基础与首屏分片，产品系列导语按总览拼贴与通用章节标题分片，服务签名响应式按平板和手机分片；
- 年限卡、JIT雷达和广州枢纽的旧临界样式已改成两行稳定导入门面，具体规则按卡片/路线、视觉/轨道、地图/详情拆分；服务详情基础结构与专题变体、首页流程卡与补充说明也分别维护；当前最大 CSS 为147行；
- `product.css` 与 `service-signature.css` 只负责有序导入，不承载具体规则。

所有样式文件均通过收紧后的200行预算。后续新增变体应进入对应领域文件；出现第二种独立变化原因时再拆，不允许重新建立大而全的聚合样式。

## 公共组件

- `Header` 从 217 行降为 67 行；桌面导航、移动导航、专题链接配置和移动菜单控制器分别维护；
- `Layout` 从 210 行降为 45 行；文档Head、悬浮联系控件、移动样式和回顶控制器分别维护；
- `PageFAQ` 的模板与样式分离；
- `AboutHonors` 的证书列表、Lightbox 控制器和样式分离；
- 案例、期刊、联系、关于页均保持数据、模板和浏览器行为边界。
- `AboutHero` 为55行编排，媒体、控制和数据条三个组件独立；`ContactForm` 为33行编排，身份字段、咨询字段和隐私同意独立；华南网络为37行编排，节点、能力带和内容配置独立；
- 退货修复工坊、JIT对比和直播电商挑战的业务清单进入 `src/data/service/`，三个视图只负责展示；
- 物流数字化页由411行降到48行，Hero、系统模块、协同链和证明条分别维护；案例详情入口由147行降到58行，Hero、项目概况和咨询区独立；新闻详情路由由282行降到65行，正文编排进入49行 `NewsArticleContent`，正文主体和相关推荐独立；新闻列表入口由209行降到58行，Hero/筛选/期刊入口与空状态分别维护；新闻FAQ进入 `src/data/news.ts`。

## Directus 与后端

`server.mjs` 现在是66行应用装配入口；环境读取与端口解析位于 `server/runtime-config.mjs`，安全头、缓存与重定向位于 `server/request-policy.mjs`，Directus依赖探测和健康响应位于 `server/health.mjs`。部署脚本会同步发布整个 `server/` 目录，避免运行入口重新吸收策略与探测实现。

运行时认证进一步拆为内容只读与联系仅创建两枚 Token；解析和权限判断位于
`server/runtime-permissions.mjs`，健康检查只验证内容读取和联系创建，不读取咨询记录。
`scripts/verify-runtime-permissions.mjs` 负责部署前严格越权探测，避免把管理级 Token 当成
网站运行凭据。Web 运行时不再回退旧共享 `DIRECTUS_TOKEN`；该变量只供建模、迁移和管理脚本临时使用。

公开内容集合、私有联系集合与全量CMS集合统一定义在 `config/cms-collections.mjs`；运行权限、
权限同步和模型契约测试共用该清单。测试会把19个模型定义与共享契约做完整集合比对，避免新增
后台栏目后漏配前端只读权限。

字体构建同样拆成35行入口、字符语料提取和字体生成三个职责。Oracle准备流程拆成55行编排入口与可复用运行函数库，保持“先验证端口、安装Directus Oracle依赖、再建立真实连接”的顺序；这些边界通过部署契约与Shell语法检查锁定，但真实Oracle仍属于外部验证。

原 `src/lib/directus.ts` 同时承担类型、SDK、查询和工具函数，现保留为兼容门面，内部拆为：

- `directus-types.ts`：CMS领域类型和Schema；
- `directus-client.ts`：URL、Token客户端、测试注入、请求与失败降级；
- `directus-queries.ts`：首页、服务、仓库、案例和新闻查询；
- `directus.ts`：稳定的统一导出路径。

关于页数据类型直接依赖 `directus-types.ts`，不再经兼容门面反向引用内容查询；源码依赖图已
确认没有导入循环。

CMS管理脚本共享 `scripts/lib/directus-admin.mjs`，避免初始化和审核同步各自实现认证、JSON解析和错误策略。CMS初始化由176行降到42行，集合模型和建模运行时独立；审核同步由224行降到91行，差异/备份、固定集合语义匹配与仓点规则分别维护；审核统计按`sort`、服务按`slug`匹配，不再把数据库ID当业务标识。审核内容也按统计、服务和仓点拆成三份事实数据。

首页内容不再集中维护：方案文案与路由解析、四类能力、媒体路径、履约步骤和FAQ进入`src/data/home/`五个领域文件；案例Logo、价值说明和FAQ进入`src/data/cases/`三个领域文件。原`home.ts`与`cases.ts`只做稳定兼容导出。关于页控制器按视频、发展历程和数字动画拆分；案例弹窗按类型、视图渲染与生命周期控制拆分。

联系API由179行降到43行编排入口，请求体读取、限流、字段验证和Directus存储分别位于 `src/lib/contact/`；邮箱、字段长度、实时播报和提交忙碌状态均有前后端契约。Oracle内容迁移则拆为Directus传输客户端、纯数据规范化/哈希和109行主流程；跨库时明确重新生成技术主键，保留原始与实际传输两份JSON，并在发现自定义关系字段时失败关闭，避免静默破坏关系。准备脚本还会验证当前Directus确实声明Oracle驱动并运行于Thin模式。

品牌公共出口也不再是数据仓库：`src/lib/brand.ts` 仅保留 6 行兼容导出，公司信息、首页能力、案例、组织资料和导航分别位于 `src/data/brand/`。`claims.ts` 从 293 行收敛为 34 行门面，履约事实、质检事实、类型和校验逻辑独立维护；履约事实内部再按时效/准确率/峰值等绩效事实与仓储/品牌/门店/SKU等规模事实分离，`fulfillment.ts`只合并稳定出口。

页面内容数据也采用同一原则：`src/data/product.ts`、`about.ts`、`publications.ts`、`service.ts` 现在只保留稳定导出路径，实际内容分别进入领域目录。服务系列与业务问题、发展历程与仓网、期刊目录与FAQ、专题类型与两套展示配置能够独立修改和审查；`src/data/` 受180行专项预算约束，避免“组件已经拆开、内容却仍集中在一个大文件”的假模块化。

测试与运维同样进入可维护性范围：原 332 行 `smoke.spec.ts` 已按首页/产品、响应式、服务专题和发布契约拆为 4 个 E2E 文件；CMS案例种子从初始化流程抽离；Nginx、Oracle切换、部署脚本与根级运行配置均受行数和契约测试约束。

`/healthz` 现在会在 1.5 秒超时内请求 Directus `/server/ping`，并分别读取内容令牌与联系令牌的
`/permissions/me`，单次检查由约20个下游请求收敛为3个。配置存在但CMS不可达、13个 active
运行集合未全部具备读取权限，或联系令牌不能创建留言时均返回503。逐集合真实读取和越权探测
保留在发布验收命令中。Web响应和外部检查统一约定 `dependencies.contactStorage: "ok"`，
`scripts/lib/health-contract.mjs` 以纯函数拒绝缺字段、`degraded`和不可达状态。

联系接口不再只信任可伪造或缺失的 `Content-Length`：应用会对实际UTF-8请求体再次执行8KB校验，Nginx也对该端点单独限流请求体。发布契约单测同时锁定原子发布静态目录、联系端点限制和Oracle失败回滚。

## 自动门禁与当前证据

- `npm run verify` 覆盖 Astro、ESLint、可维护性预算、资源完整性、Vitest 与生产构建；
- `npm run verify:release` 额外覆盖桌面/移动 Playwright 和正式域名契约；最新通过数量与发布结果统一记录在根目录 `DEV_STATE.md`，本文件不再复制易过期的计数；
- 360px产品页回归测试锁定“移动端不请求 `.woff2` 品牌字体”，避免字体策略回退；
- Nginx、PostgreSQL、Oracle 19C、部署与健康检查脚本均有对应的静态或契约检查；
- 被Git忽略的普惠体不再依赖当前工作机：`scripts/prepare-fonts.mjs` 从站点源码提取1338个实际码点，由锁定依赖为400/900字重各生成一个约200KB的站点子集；全字库冗余不进入发布产物，`precheck:assets`、`prebuild` 会自动重建。
- 合作品牌与关于页发布素材已纳入 Git；GitHub Actions 会从干净检出运行格式、生产依赖审计和完整发布门禁。
- PostgreSQL、Oracle 与 Directus 附件备份使用 root 专用 `EnvironmentFile`，不执行应用目录配置；归档具备 SHA-256、并发锁、保留策略和隔离恢复测试，但必须由部署人员在服务器安装并实跑。

## 仍然存在的维护债务

### P1

1. 历史性能采样见 `docs/PERFORMANCE_BASELINE.md`；只有真实访问或发布指标出现退化时才启动性能优化，不把实验室建议自动升级为开发任务。

### P2

1. 新闻当前没有已审核公开文章；恢复内容时应新增独立列表、分页与正文块渲染器，不把这些职责重新塞回58行页面入口或49行正文编排器。
2. 当前最大的三份样式是 `home-capability/responsive-mobile.css`（147行）、`service-signature/base.css`（147行）和 `product/service-series-spread.css`（146行）。它们分别只维护能力区手机断点、服务签名共享基础和产品系列展开布局；出现第二套断点、视觉体系或独立交互状态时再拆分。
3. 当前最大的业务组件是 `NewsLandingIntro`（113行）、`AboutHistory`（111行）和 `Footer`（111行）。三者分别只维护新闻列表导语、关于页历程和全站页脚；新增筛选状态机、第二种历程数据源或动态页脚配置时必须先建立新边界。此前的 AboutHero、华南网络和联系表单已完成职责拆分，不再列为债务。
4. 当前最大的内容文件 `src/data/publications/issues.ts` 为166行，只保存期刊事实清单；最大的测试 `contact.test.ts` 为159行，只验证联系接口契约；最大的发布入口 `scripts/deploy.sh` 为156行，只编排原子发布。它们继续观察，但不因行数机械切碎；一旦加入第二种内容类型、无关API或数据库迁移职责，必须独立成模块。

## 职责状态，而不只是行数状态

状态含义：`门禁通过`表示本地职责边界和自动检查已成立，不等同于线上验证；`观察`表示当前仍是单一职责但接近阈值；`待外部验证/输入`表示必须依赖服务器、数据库或业务内容。

| 领域           | 当前边界                                                       | 状态       | 下一触发条件                          |
| -------------- | -------------------------------------------------------------- | ---------- | ------------------------------------- |
| 页面入口       | 只负责取数、Schema与模块编排；单页150行门禁                    | 门禁通过   | 入口重新出现大段DOM或浏览器逻辑       |
| 业务组件       | 首页、产品、关于、案例、联系、期刊、数字化与12条专题按业务拆分 | 门禁通过   | 组件加入第二种独立业务流程            |
| 浏览器控制器   | Header、FAQ、弹窗、产品页、能力动效分别维护                    | 门禁通过   | 同一控制器开始绑定无关DOM域           |
| 事实与内容数据 | 品牌、导航、履约、质检、首页、产品、CMS审核配置分域并保留门面  | 门禁通过   | 同一事实出现第二个手写来源            |
| CMS读取        | 类型、客户端、查询、兼容出口分层                               | 门禁通过   | 引入缓存/快照时新增策略层             |
| CMS管理        | 管理客户端、模型、初始化、语义同步、仓点规则与审核数据分离     | 门禁通过   | 新增第二类特殊迁移规则                |
| 联系API        | HTTP、限流、验证、存储与43行路由编排分离                       | 门禁通过   | 增加通知/CRM时新增应用服务层          |
| Web运行时      | 应用装配、运行配置、请求策略和健康探测分离                     | 门禁通过   | 增加会话、队列或第二类依赖探测        |
| 构建工具       | 字体入口、字符语料和生成器分离                                 | 门禁通过   | 增加第二种字体产物或远程下载策略      |
| 样式           | 聚合入口只导入，具体规则按页面域、断点或视觉变体维护           | 观察       | 最大三个CSS出现第二套独立布局         |
| 测试           | 单元、响应式、页面流程、专题、候选环境和正式域名契约分域       | 门禁通过   | 新增关键DOM/ARIA/发布契约没有对应测试 |
| 发布与恢复     | 原子发布、健康检查、CMS迁移备份与隔离恢复已在验收站实跑        | 验收通过   | 正式发布前重新核对目标环境            |
| 内容运营       | 新闻空状态诚实显示，未伪造内容                                 | 待业务输入 | 有审核文章后增加列表、分页与证据字段  |

### 外部环境待办

1. 本次 staging CMS 迁移使用的数据库与附件备份已经过校验和及隔离恢复/解包验证；持续性定时备份是否安装不作为当前代码状态结论；
2. 原子部署已在验收服务器实跑，站点通过严格健康检查并运行于`current`发布软链；首次实跑发现的PM2工作目录与空软链回滚目标问题已修复并加入契约测试，自动回滚分支仍应在下一次受控发布中复验；
3. `56xyy.com` 仍解析到旧服务器，其健康、CMS、robots、sitemap和llms路径均回退为旧站HTML；DNS、证书、Nginx、正式SEO/AEO和表单落库尚未验证；
4. Oracle 19C迁移只有脚本静态检查，没有真实数据库连接、数据核对和恢复记录。

## 拆分原则

每次只移动一个职责边界，保持视觉、文案、接口、路由和业务口径不变。拆分后至少运行：

```bash
npm run verify
npm run test:e2e
npm audit --omit=dev
```

不以“文件越小越好”为目标；只有独立职责、独立变化原因或独立测试契约存在时才拆分。
