# 项目状态

更新时间：2026-08-13

## 2026-08-13 本轮发布

- 本轮目标是把网站、Directus 中文管理模型、FAQ/案例/服务页聚合编辑和统一运营口径同步到阿里云验收站 `https://wz.tomatopia.top`；
- 验收站应用服务器为 `47.82.105.103`，Directus 当前明确使用 PostgreSQL 16，本轮不执行 Oracle 迁移；
- 已在变更前生成并校验 PostgreSQL 自定义格式备份，Oracle Database 19c 继续作为独立数据库服务器的后续迁移目标；
- 首页业务数据统一为 `150+` 服务品牌、`54万㎡` 直营仓储；案例改为每品牌一条记录内维护全部指标，FAQ 按页面聚合，服务专题页在同一记录维护指标、能力和图片；
- 本地验证通过：Astro 284 个输入 0 错误、ESLint、424 文件可维护性预算、55 个引用资源与 104 个部署资源、15 个测试文件 74 项单测、27 项浏览器测试、3 项正式域名契约测试和生产构建全部通过；
- 浏览器回归确认 Directus 中文分组、首页单例、17 个 FAQ 页面、100 条 FAQ、案例聚合指标和服务页聚合字段可用，390px 移动端无结构溢出；
- 变更前已生成并校验 PostgreSQL 自定义格式备份；随后完成 19 个 Directus 集合的幂等建模与迁移，6 个案例、100 条 FAQ、12 个服务页及其聚合字段验证通过；
- 验收站已原子发布到 `/var/www/xyy-web/releases/20260813T182300Z`，首页、`/healthz`、Directus ping、robots、sitemap 和 llms.txt 外部检查通过；
- GitHub `main` 的应用提交为 `9d4b43c`；提交前已检查提示词、环境变量、Token 与私钥，均未进入仓库；
- 本轮不修改正式域名、DNS、Nginx，也不迁移数据库。

## 当前结论

新版官网运行在验收服务器 `47.82.105.103`，验收域名为 `https://wz.tomatopia.top`，当前发布目录为 `/var/www/xyy-web/releases/20260813T182300Z`。Directus 内容模型已升级，数据库仍是 PostgreSQL 16；Oracle 19c 迁移保持“脚本已准备、独立数据库环境待执行和恢复验证”，不能描述为已上线。

正式域名 `https://56xyy.com` 已由另一台服务器提供新版页面和健康接口，但尚未同步本轮完整资源包与 Directus 聚合内容模型。因此“正式站可访问”不能等同于“与验收站版本一致”。

## 2026-08-13 正式站与验收站浏览器对比

- 使用真实 Chromium 依次访问首页、产品、案例、UR 案例详情、关于、新闻、森林期刊、鞋服云仓和联系页；两站核心页面均返回 200，桌面端无横向溢出，正式站首页和产品页在 390px 下也无横向溢出；
- 正式站已显示 `150+` 品牌、`54万㎡` 仓储和 6 个首页案例，健康接口、Directus ping、robots、sitemap、llms.txt 均通过，canonical 指向 `https://56xyy.com`；
- 对本地发布依赖的 157 个业务素材逐一请求：验收站 157/157 可用，正式站 111 个返回 404。缺失集中在 `public/logos/` 的合作品牌 Logo，以及 `public/about/history/`、`public/about/honor/` 的关于页素材；
- 正式站 `/cases` 未输出 6 个独立案例详情链接，UR 详情缺少验收站已有的服务范围标签，说明正式站 Directus 尚未同步本轮案例聚合模型与迁移数据；
- 正式站与验收站共享的仓储、系统看板、智能寄件和期刊封面素材校验和一致，说明主要差异不是源码图片版本，而是被 Git 忽略、需由部署包单独恢复的业务素材，以及两套独立 Directus 的内容版本；
- 浏览器中未发现新增 JavaScript 控制台异常；案例页 404 控制台信息来自上述缺失静态素材。

正式站下一步需要由其部署团队完成两项工作：一是把验收站已验证的 `public/logos/` 与 `public/about/` 持久资源同步到正式服务器；二是在正式 Directus 备份后执行本轮幂等建模、内容迁移和 `cms:verify`，再发布同一 Git 提交。不能只重新构建 Git 仓库，因为上述两类目录按设计不进 Git。

## 本地候选版本证据

- Astro检查：261个项目输入，0错误、0警告、0提示；
- ESLint：通过；
- Prettier：全部目标文件通过，历史40个格式告警已清零；
- 可维护性预算：395个项目文件通过，覆盖源码、测试、自动化、部署脚本、服务端运行模块和根运行配置；
- 静态资源完整性：55个本地资源引用跨331个源码文件全部存在；
- Vitest：13个测试文件、57项通过；
- Astro SSR生产构建：通过；
- Playwright：27项通过、3项按项目配置有意跳过，覆盖桌面、移动、中间断点、12条服务专题、关键交互、SEO/AEO和后端失败路径；
- 正式域名独立Playwright契约3项通过：核心页无`noindex`，canonical与OG URL指向`https://56xyy.com`，robots/sitemap/llms.txt使用正式源站，www/旧域名/旧路径跳转正确；
- 360px产品页测试额外断言移动端不会请求 `.woff2` 品牌字体；
- `npm audit --omit=dev`：0个生产依赖漏洞；
- Codex Security修复范围最终复扫：0项发现；
- 部署、Nginx、PostgreSQL、Oracle 19C和健康检查脚本：本地静态语法通过。

`npm run verify:release` 已于 2026-08-09 从当前工作树重新完整执行通过，以上结果不是由分散的历史命令拼接得出。

2026-08-09 18:14（Asia/Shanghai）发布后复核：验收站 `/healthz` 返回 `dependencies.contactStorage: "ok"`，`/llms.txt` 返回 `text/plain`，站点、CMS ping、robots和sitemap全部通过。正式域名仍由旧服务器提供内容，其切换状态与验收站发布状态继续分开记录。

## 可维护性状态

详细矩阵见 [MAINTAINABILITY.md](MAINTAINABILITY.md)。本轮新增和纠正的状态：

- 首页Hero、FAQ、履约区的模板、数据、样式和浏览器行为已分离；
- 首页能力统计父组件拆成6个业务子组件；374行动画控制器拆成运行时、年限卡、统计卡、流程线和滚动注册；
- 产品服务系列拆为父编排、单项、商品整理画廊和图标组件；
- 产品保障机制的五套SVG从内容模板中抽离；
- Header拆为桌面导航、移动导航、专题链接配置和菜单控制器；
- 关于页荣誉Lightbox的DOM、控制器和样式分离；
- 12套服务专题视觉变体进入12个独立组件，三族父组件只负责分派；
- 仓储专题视觉族与产品服务系列的两份临界CSS已继续按变体拆为10个职责文件；
- Directus类型、客户端和查询拆成独立模块，`directus.ts`保留为兼容导出门面；
- `Layout`拆为文档Head、悬浮联系控件、样式和回顶控制器；
- `brand.ts`与`claims.ts`改为稳定门面，公司、首页、案例、组织、导航、履约事实和质检事实按领域维护；履约事实继续按履约绩效与企业规模拆分，原`FULFILLMENT_CLAIMS`出口保持兼容；
- 12条服务专题的独有展示全部从路由抽到 `components/service/unique/`，路由入口统一为73–80行；
- 原两个450行级聚合样式及物流数字化、产品目录/响应式样式继续按领域、断点和视觉职责拆分；年限卡、JIT雷达、广州枢纽、服务详情和首页流程样式已进一步拆为稳定导入门面与布局/变体/说明职责文件；当前最大CSS为147行；
- 首页Hero按主体与响应式拆分，履约区按布局、步骤和响应式拆分；案例画廊按面板与详情拆分，案例弹窗按外壳、首图、正文和响应式拆分；运到运营页按标题、界面、流程场景和响应式拆分；直播履约与JIT雷达拆为两个视觉域；产品服务选择器拆为导语、问题列表和方案面板；首页统计拆为布局、年限卡、指标和装饰；服务详情拆为基础结构与变体规则；
- 产品、关于、期刊与专题服务配置不再各自堆在单个数据文件中，现按服务系列/业务问题/保障、历程/仓网/FAQ、期刊目录/支持内容、类型/体验文案/视觉配置分域，并保留原导入路径作为兼容门面；
- 首页内容继续按方案、能力、媒体、履约和FAQ拆为5个领域文件；案例内容按Logo、价值说明和FAQ拆为3个领域文件，`home.ts`与`cases.ts`只保留稳定导出；
- 关于页浏览器入口只装配视频、历程与数据计数三个控制器；案例弹窗将类型、纯视图渲染和打开/关闭/历史控制分离；首页能力指标样式将通用排版与业务卡片变体分离；
- 关于页Hero正文为底部数据条预留空间并建立明确层级；真实浏览器测试锁定静音按钮可点击和ARIA状态切换，修复低高度桌面视口中数据条拦截按钮的问题；
- 原332行E2E文件按首页/产品、响应式、服务专题和发布契约拆为4个文件；
- 物流数字化页由411行降到48行，Hero、系统模块、协同链和证明条分别维护；案例详情入口由147行降到58行，Hero、概况和咨询模块独立；新闻详情路由由282行降到65行，正文编排降到49行并拆出主体与相关推荐；新闻列表入口由209行降到58行，首屏入口与空状态独立；
- 联系API由179行降到43行，请求读取、限流、字段验证和Directus存储分离；
- CMS初始化由176行降到42行，集合定义与管理运行时独立；审核同步主流程由224行降到91行，统计按`sort`、服务按`slug`匹配，审核事实数据、通用差异/备份运行时与仓点规则分别维护；
- Oracle内容迁移拆为109行编排、Directus传输客户端和纯数据规范化/哈希；迁移重新生成跨库技术主键，保存原始/实际传输快照，并在发现自定义关系字段时失败关闭；
- CMS服务器初始化Shell由215行降为约47行编排，安装、配置、Directus和PM2步骤进入可复用函数库；
- 关于页Hero已拆为55行编排以及媒体、控制和数据条三个组件；联系表单已拆为33行编排以及身份字段、咨询字段和隐私同意；华南仓网拆为37行编排以及节点、能力带和独立内容配置；
- 退货修复工坊、JIT对比和直播电商挑战的内容配置已迁入 `src/data/service/`，视图不再同时维护业务清单；
- `server.mjs` 已降为66行应用装配，运行配置、请求安全策略和依赖健康检查分别位于 `server/`；部署脚本同步发布该运行目录；
- 字体准备入口已降为35行，字符语料提取与字体生成分别维护；Oracle准备入口已降为55行，安装、连接验证、环境、建模与PM2运行函数进入 `deploy/oracle19c/lib/prepare-runtime.sh`；
- 公开规模、履约和质检数字全部由 `src/lib/claims/` 分域注册，单元测试扫描页面、组件和内容配置，阻止同一运营口径重新出现手写副本；
- 新增 `check:maintainability` 并纳入 `npm run verify`，避免大文件和超长内联代码回流。

当前最大业务组件为113行的新闻列表导语，最大浏览器控制器为112行；当前最大CSS为147行。可维护性门禁扫描391个项目文件，CSS上限由300行分阶段收紧至200行，内容数据文件使用180行专项预算，并继续对页面入口、公开API、CMS初始化/同步、Oracle迁移、测试和Shell使用专项预算。剩余较大的文件均按单一职责标记为“观察”，不是“永久完成”；只有出现第二种数据源、布局、流程或交互状态机时才继续拆分，避免为行数制造无意义碎片。“门禁通过”只表示本地边界与检查成立，不代表已完成线上验证。

## 后端与健康状态

- 联系表单缺少Directus配置或落库失败时返回503，不返回虚假成功；
- 联系接口路由只负责请求编排，HTTP读取、限流、业务校验和Directus存储已独立，可分别测试和替换；
- 可选邮箱现在由服务端校验，字段长度在页面与API两侧保持一致；提交结果使用ARIA实时区域，提交期间暴露忙碌状态；
- 联系表单同时校验声明长度和实际UTF-8请求体大小，反向代理将该接口限制为8KB；限流标识优先使用可信代理写入的真实IP，避免伪造转发前缀绕过；
- `/healthz` 会真实请求Directus `/server/ping`，1.5秒超时；配置存在但依赖不可达时返回503；
- Web服务与外部健康检查统一使用 `dependencies.contactStorage: "ok"`；纯函数契约会拒绝旧版缺少依赖字段、`degraded`或不可达状态，避免健康服务实际正常却因枚举不一致触发错误回滚；
- E2E刻意使用不可达Directus，验证查询降级、健康失败关闭和页面基础结构；
- CMS内容仍采用实时读取，不使用统计/服务陈旧快照；发布必须把CMS健康检查作为硬门槛。

## 发布脚本状态

- Nginx静态资源统一读取 `/var/www/xyy-web/current/dist/client`，与原子发布软链保持同一版本，避免SSR HTML与旧静态资源混用；
- `/api/contact` 有独立8KB代理限制，不影响Directus后台所需的较大上传限制；
- Oracle切换脚本在目标PM2启动失败或健康检查失败时，都会恢复目标暂存端口、重启PostgreSQL版Directus并验证回滚健康；
- Oracle内容迁移按业务字段核对，不依赖PostgreSQL与Oracle之间保持相同的自增主键；当前自定义集合若出现关系字段，脚本会中止并要求显式关系映射；
- Oracle准备阶段会从当前安装的Directus解析实际版本和`oracledb`可选依赖；缺少Oracle驱动或不是Thin模式时提前终止。本轮只读核对确认Directus 12.1.1所用API包声明`oracledb 6.10.0`，服务器驱动处于Thin模式；六个当前自定义集合均未发现关系字段，但真实Oracle连接仍未执行；
- 正式域名契约与健康检查同时验证robots、sitemap和llms的Content-Type；`llms.txt`还锁定`54万㎡ / 150+品牌 / 45万+ SKU`，避免旧站HTML回退或业务旧口径以HTTP 200蒙混通过；
- Vitest发布契约会锁定上述Nginx、原子软链和Oracle回滚条件，防止后续脚本回退。
- `verify:release` 现在除候选环境桌面/移动E2E外，还会以 `PUBLIC_SITE_URL=https://56xyy.com` 重新构建并运行正式源站契约；正式域名模式下禁止`noindex`或错误canonical。
- robots发布检查按User-agent分组判断，GPTBot等特定爬虫的业务策略不会被误判成全局`User-agent: *`封站。

## 性能与媒体

- Lighthouse桌面三次采样中位数：首页性能83、产品页91、关于页90；LCP分别约2.16秒、1.80秒、1.93秒；产品页最终单次复验为性能92、无障碍100、最佳实践100、LCP 1.8秒、TBT 0、CLS 0；
- 最新桌面单次采样为首页97、产品页97、关于页99；移动端不下载品牌字体，产品页请求总数57→17、FCP 7.7秒→1.4秒、性能58→77。本轮移动采样为首页86/模拟LCP3.7秒、产品页77/6.3秒、关于页76/5.6秒，真实追踪LCP约1.63秒、1.33秒、1.36秒；三页无障碍和最佳实践均100；
- 本地SEO分数受候选环境 `noindex` 影响，正式域名必须重新采样；
- 产品首屏已提供960/1440两档响应式WebP、延迟加载非首屏动效并对长页面区块启用渲染隔离；产品页原三次采样中位数约77分、LCP约3.33秒，现为91分、1.80秒；
- 普惠体900字重由1.15MB单文件改为按Unicode范围分片加载，原文件可恢复地归档到未发布资源目录；产品履约图已由1.69MB JPG切换为270KB WebP；关于页视频由17MB/720p切换为7.5MB/540p；
- 字体目录虽然不提交Git，但 `prepare:fonts` 会从源码提取1338个站点码点，为400/900字重各生成一个约200KB子集；全字库冗余不会进入发布目录。桌面端异步启用品牌字体，移动端使用系统中文字体，避免字体换装造成首屏重复布局；
- 约43MB未发布原始媒体已移到 `resources/original-media/unpublished-public/`，没有直接删除；
- 公开目录仍包含约132MB期刊PDF，它们是当前页面明确链接的业务资产。

## 环境状态

| 环境         | 地址 / 位置                | 状态                           |
| ------------ | -------------------------- | ------------------------------ |
| 本地候选版本 | `/home/yj/XYY-GEO/website` | 全部门禁通过                   |
| 验收域名     | `https://wz.tomatopia.top` | 20260813T182300Z 已部署，严格健康检查通过 |
| 正式域名     | `https://56xyy.com`        | 新版页面可用，但资源包与 CMS 内容模型未完全同步 |
| CMS数据库    | PostgreSQL                 | 当前实际运行数据库             |
| Oracle 19C   | 独立数据库服务器方案       | 脚本已准备，未实迁             |

### 2026-08-09 发布后复核

- `wz.tomatopia.top` 解析到 `47.82.105.103`，首页、CMS ping、robots 与 sitemap 可访问；
- 验收站当前运行 `/var/www/xyy-web/releases/20260809T100600Z`，`current` 软链、PM2工作目录与Nginx静态资源目录均指向同一版本；
- `/healthz`、Directus ping、robots、sitemap和llms.txt全部通过严格检查，`/healthz`明确返回联系存储依赖健康；
- `56xyy.com` 与 `www.56xyy.com` 仍解析到 `139.224.11.72`；首页是旧版静态站，缺少新版 canonical；
- 正式域名的 `/healthz`、`/cms/server/ping`、`/robots.txt`、`/sitemap.xml` 和 `/llms.txt` 均被旧站统一回退为HTML，虽然部分HTTP状态为200，但内容契约全部失败；
- 因此“验收站已发布”与“正式域名已切换”必须继续分开记录。

验收服务器确认：PM2 的 `xyy-web`、`xyy-cms` 在线，Web仅监听 `127.0.0.1:4321`，Directus版本12.1.1，当前数据库仍为PostgreSQL。Nginx静态资源已切换到 `current/dist/client`，进程工作目录指向当前发布目录。PostgreSQL备份timer仍未安装，PostgreSQL和Oracle备份目录仍不存在。

首次原子发布实跑暴露了两个编排问题：PM2 reload不会更新旧进程工作目录，以及不存在的`current`路径会被`readlink -f`误记为回滚目标。服务器状态已人工收口，部署脚本已改为只读取真实软链、删除并重建Web进程，并用联系存储依赖状态作为内部启动门槛；对应契约测试已补充。

2026-08-09 使用只读 CMS 凭据复核的内容数量为：`homepage_stats=8`、`services=3`、`warehouses=12`、`cases=6`、`news=0`、`contact_leads=0`。本轮将通过幂等初始化和迁移脚本升级现有 PostgreSQL 内容模型，不涉及 Oracle。

## 已准备但未执行

- `deploy/postgresql/`：备份、systemd timer、恢复测试；
- `deploy/oracle19c/`：安装、迁移、并行验证、切换、回滚和备份；
- `deploy/nginx-56xyy.conf`、`deploy/nginx-wz-redirect.conf`：正式域名与旧域名跳转草案。

## 发布前外部待办

1. 在服务器安装数据库备份任务，并完成一次异机备份和真实恢复演练；
2. 在下一次发布时再次验证自动回滚分支，确认修正后的PM2工作目录切换与回滚目标选择；
3. 完成 `56xyy.com` DNS、证书、Nginx、canonical、robots、sitemap和正式Lighthouse检查；
4. Oracle 19C独立服务器就绪后，完成并行实例迁移、数据核对、备份、恢复和回滚验证。

## 发布边界

本轮已完成验收服务器部署与GitHub `main`同步；没有切换正式域名、修改DNS或执行生产数据库迁移。真实环境变量、Token、数据库凭据和密钥没有进入仓库。
